# dsh-remote-workspace — bundle patch.
# 与 package.json 的 dsh.bundle.patch 对应：`dsh plugin add` 会把这一行插入所选
# profile 的 cordis 配置。id 保持短名，name 使用 npm 包名。
- insert:
    - id: remote-workspace
      name: '@dsh-external/dsh-remote-workspace'
